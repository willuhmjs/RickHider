export interface AvailableMeta {
    title: string;
    description: string;
    themeColor: string;
    type: 'summary' | 'summary_large_image';
    imageUrl: string;
    url: string;
    siteName: string;
}